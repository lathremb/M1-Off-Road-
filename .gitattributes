# Normalise line endings so the repo stays consistent between Windows and the
# Linux builders on Vercel.
* text=auto eol=lf

# Binary — never touch these.
*.woff2 binary
*.jpg   binary
*.jpeg  binary
*.png   binary
*.webp  binary
*.avif  binary
*.ico   binary
